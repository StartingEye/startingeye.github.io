---
layout: post
title: "[TIL] 데이터베이스와 Python 연동"
date: 2026-03-17 10:00:00 +0900
categories: [TIL, DB]
tags: [security, DB, vscode, MySQL]
---

## 📅 오늘 학습한 내용
파이썬을 활용하여 MySQL과 연동하여 입력 및 출력하는 방법을 배움
코드를 수정하여 다른 입력값을 추출 해 낼수 있었다

---

## 1. Python Code
```
import mysql.connector

conn = mysql.connector.connect(
    host="localhost",
    user="root",
    password="dodari0114@",
    database="filter_db"
)

cursor = conn.cursor()

#cursor.execute("update users SET id = 11 WHERE id = 10")
conn.commit()

cursor.execute("SELECT * FROM students where grade =2")
print(cursor.fetchall())

cursor.close()
conn.close()
```
---

## 💡 느낀 점
보다 편하게 MySQL을 사용할 수 있음을 알게 되었다
입력값 검증을 잘 해야 보안 사고가 발생하지 않음을 알수 있었다.

## 💡 배운 내용
Python을 이용해서 DB 및 MySQL을 접속 하여 데이터를 추출 및 입력 할수 있다는 것을 배울수 있었다.


## 앞으로 계획
입력값 검증과 데이터 베이스 보안을 더 공부하여 안전한 데이터 베이스 구축을 해보겠다.

